<?php

namespace App\Documentation;

/**
 * @OA\Tag(
 *     name="Authentication",
 *     description="Endpoints related to user authentication (generate access token, etc.)"
 * )
 *
 * @OA\Tag(
 *   name="Regimes",
 *   description="Endpoints for managing hotel regimes (all-inclusive, half-board, etc.)"
 * )
 *
 * @OA\Tag(
 *     name="Rooms",
 *     description="Endpoints for managing hotel rooms (listing, availability, details)"
 * )
 *
 * @OA\Tag(
 *     name="Reservation",
 *     description="Endpoints for managing hotel guests (CRUD operations, reservations)"
 * )
 *
 * @OA\Tag(
 *   name="Payments",
 *   description="Endpoints for managing payments (credit card, bank transfer, etc.)"
 * )
 */
class SwaggerTagsDocumentation
{
  // Este arquivo serve apenas para documentação Swagger (OpenAPI).
  // Não precisa de código funcional, apenas das annotations acima.
}
